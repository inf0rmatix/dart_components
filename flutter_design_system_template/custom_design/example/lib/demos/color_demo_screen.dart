import 'package:flutter/material.dart';

class ColorDemoScreen extends StatelessWidget {
  const ColorDemoScreen({super.key});

  @override
  Widget build(BuildContext context) => const Center(
    child: Text('Color Demo Screen'),
  );
}
